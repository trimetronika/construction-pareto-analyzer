{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd ../frontend && bun install && vite build --outDir=../backend/frontend/dist"
  },
  "dependencies": {
    "encore.dev": "^1.48.12",
    "groq-sdk": "^0.20.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}